# 🚀 Запуск проекта Kavnt

## 📋 Требования

- Python 3.10+
- Node.js 18+
- MongoDB
- Git

## 🗂️ Структура проекта

```
kavnt_project_test/
├── back/           # Django DRF бэкенд
├── front/          # Next.js фронтенд
└── README_SETUP.md # Этот файл
```

## 🛠️ Настройка и запуск

### 1. Клонирование и подготовка

```bash
# Клонировать репозиторий (если нужно)
git clone <repository-url>
cd kavnt_project_test
```

### 2. Настройка бэкенда (Django)

```bash
# Переходим в папку бэкенда
cd back

# Создаем виртуальное окружение
python -m venv .venv

# Активируем виртуальное окружение
# Windows:
.venv\Scripts\activate
# Linux/macOS:
# source .venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt

# Копируем файл конфигурации
cp .env.example .env

# Настраиваем .env файл (при необходимости)
# Основные настройки:
# MONGODB_HOST=mongodb://127.0.0.1:27017
# MONGODB_NAME=kavnt
# SECRET_KEY=your-secret-key-here
```

### 3. Настройка MongoDB

```bash
# Запуск MongoDB (если установлен локально)
mongod

# Или используйте Docker:
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

### 4. Создание тестовых данных

```bash
# Находясь в папке back/ с активированным виртуальным окружением:
python mock_data.py
```

### 5. Запуск бэкенда

```bash
# Находясь в папке back/ с активированным виртуальным окружением:
python manage.py runserver

# Бэкенд будет доступен по адресу: http://127.0.0.1:8000
# API документация: http://127.0.0.1:8000/api/docs/
```

### 6. Настройка фронтенда (Next.js)

```bash
# Открываем новый терминал и переходим в папку фронтенда
cd front

# Устанавливаем зависимости
npm install

# Копируем файл конфигурации
cp .env.local.example .env.local

# Настраиваем .env.local файл:
# NEXT_PUBLIC_API_URL=http://localhost:8000
```

### 7. Запуск фронтенда

```bash
# Находясь в папке front/:
npm run dev

# Фронтенд будет доступен по адресу: http://localhost:3000
```

## 🎯 Полные команды запуска

### Быстрый старт (все команды)

```bash
# === Терминал 1: Бэкенд ===
cd back
python -m venv .venv
.venv\Scripts\activate  # Windows
pip install -r requirements.txt
cp .env.example .env

# Очистка и создание данных (при необходимости)
python clear_db.py  # Очистка базы
python mock_data.py  # Создание тестовых данных

python manage.py runserver

# === Терминал 2: Фронтенд ===
cd front
npm install
cp .env.local.example .env.local
# Отредактируйте .env.local: NEXT_PUBLIC_API_URL=http://localhost:8000
npm run dev

# === Терминал 3: MongoDB (если не запущен) ===
mongod
# или через Docker:
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

## 📱 Доступ к приложению

- **Фронтенд**: http://localhost:3000
- **Бэкенд API**: http://127.0.0.1:8000
- **API Документация**: http://127.0.0.1:8000/api/docs/

## 👤 Тестовые пользователи

После выполнения `mock_data.py` будут созданы:

| Роль | Логин | Пароль | Имя | Группы |
|------|-------|--------|-----|--------|
| Администратор | admin | admin123 | Александр Админов | - |
| Учитель 1 | teacher1 | teacher123 | Иван Иванов | ИТ-101, ИТ-102 |
| Учитель 2 | teacher2 | teacher123 | Мария Петрова | ИТ-201, ИТ-202 |
| Студент 1 | student1 | student123 | Петр Сидоров | ИТ-101 |
| Студент 2 | student2 | student123 | Анна Козлова | ИТ-102 |
| Студент 3 | student3 | student123 | Дмитрий Новиков | ИТ-201 |
| Студент 4 | student4 | student123 | Елена Белова | ИТ-202 |
| Студент 5 | student5 | student123 | Михаил Волков | ИТ-301 |

### 🏫 Структура групп

- **Студенты** привязаны к одной группе (`group_id`)
- **Учителя** могут вести несколько групп (`group_ids`)
- **Администраторы** не привязаны к группам

### 📝 Запуск мокапа

```bash
# Единый универсальный мокап (рекомендуется)
python mock_data.py
```

### 🗑️ Очистка базы данных

**⚠️ ВНИМАНИЕ:** Это удалит ВСЕ данные из базы!

```bash
# Полная очистка всех данных
python clear_db.py

# Подтверждение: потребуется ввести 'DELETE ALL'
```

**Команды для полного сброса:**
```bash
# 1. Очистить базу данных
python clear_db.py

# 2. Создать новые данные
python mock_data.py
```

## 🎮 Что доступно в проекте

### Для студентов:
- 📚 Просмотр треков и уроков
- 📖 Чтение лекций
- 💻 Решение задач с тестами
- 🧩 Решение puzzle-заданий (сборка кода из блоков)
- 📊 Отслеживание прогресса

### Для учителей:
- ➕ Создание треков, лекций, задач
- 🧩 Создание puzzle-заданий
- 👥 Управление группами студентов
- 📈 Просмотр прогресса студентов

### Для администраторов:
- 👥 Управление всеми пользователями
- 🏫 Управление группами
- 🔧 Полный доступ ко всем функциям

## 🔧 Возможные проблемы

### MongoDB не запускается
```bash
# Проверьте, установлен ли MongoDB
mongod --version

# Если нет, установите или используйте Docker
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

### Ошибка подключения к MongoDB
```bash
# Проверьте, что MongoDB запущен
# Проверьте настройки в .env файле
# Убедитесь, что порт 27017 доступен
```

### Ошибки с зависимостями
```bash
# Для бэкенда:
pip install --upgrade pip
pip install -r requirements.txt

# Для фронтенда:
rm -rf node_modules package-lock.json
npm install
```

### Порт уже занят
```bash
# Измените порт в runserver:
python manage.py runserver 8001

# Или измените порт в Next.js:
npm run dev -- -p 3001
```

## 📝 Разработка

### Добавление новых puzzle-заданий
1. Перейдите в `/admin/puzzles/new`
2. Заполните форму
3. Добавьте блоки кода
4. Укажите правильный порядок

### Создание новых треков
1. Перейдите в `/admin/tracks/new`
2. Создайте трек
3. Добавьте уроки (лекции, задачи, puzzle)

## 🎉 Готово к работе!

После выполнения всех шагов у вас будет полностью работающая образовательная платформа с:
- ✅ Аутентификацией пользователей
- ✅ Тремя типами уроков (лекции, задачи, puzzle)
- ✅ Системой тестирования кода
- ✅ Интерактивными puzzle-заданиями
- ✅ Прогрессом студентов
- ✅ Административным интерфейсом

Удачи в использовании! 🚀
